import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#1B5E20",
    },
    secondary: {
      main: "#6D4C41",
    },
  },
});

export default theme;
