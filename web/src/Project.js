import AppBarLayout from "./components/AppBarLayout";
import React from "react";

function Project() {
  // TODO:
  // let {projectId} = useParams();
  // let project = useProject(projectId);
  return <AppBarLayout>TODO: project page</AppBarLayout>;
}
