import AppBarLayout from "./components/AppBarLayout";
import React from "react";

function Reviewer() {
  // TODO:
  // let {reviewerId} = useParams();
  // let reviewer = useReviewer(reviewerId);
  return <AppBarLayout>TODO: reviewer page</AppBarLayout>;
}
