import AppBarLayout from "./AppBarLayout";
import React from "react";

export default function Address() {
  // TODO:
  // let {address} = useParams();
  // let address = useAddress(address);
  return <AppBarLayout />;
}
